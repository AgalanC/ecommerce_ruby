<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Account</title>
  <%= csrf_meta_tags %>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
  <div class="flex items-center justify-center min-h-screen bg-gray-100">
    <div class="w-full max-w-md p-8 space-y-6 bg-white rounded-lg shadow-lg">
      <h2 class="text-3xl font-extrabold text-center text-gray-900">My Account</h2>
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }, class: 'space-y-6') do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>

        <div class="rounded-md shadow-sm -space-y-px">
          <div class="mb-4">
            <%= f.label :name, "Name", class: 'block text-sm font-medium text-gray-700' %>
            <%= f.text_field :name, placeholder: "Name", class: 'mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm' %>
          </div>
          <div class="mb-4">
            <%= f.label :username, "Username", class: 'block text-sm font-medium text-gray-700' %>
            <%= f.text_field :username, placeholder: "Username", class: 'mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm' %>
          </div>
          <div class="mb-4">
            <%= f.label :email, "Email", class: 'block text-sm font-medium text-gray-700' %>
            <%= f.email_field :email, placeholder: "Email", class: 'mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm' %>
          </div>
          <div class="mb-4">
            <%= f.label :address, "Address", class: 'block text-sm font-medium text-gray-700' %>
            <%= f.text_area :address, placeholder: "Address", class: 'mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm' %>
          </div>
          <div class="mb-8">
            <%= f.label :province, "Province", class: 'block text-sm font-medium text-gray-700' %>
            <%= f.select :province, options_from_collection_for_select(Tax.all, "province", "province", selected: (resource.orders.last&.tax&.province || "")), prompt: "Select Province", class: 'mt-1 block w-full px-3 py-2 border-2 border-gray-500 rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm' %>
          </div>
        </div>

        <div class="pt-4">
          <%= f.submit "Update", class: 'group relative w-full flex justify-center py-2 px-4 border border-transparent text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 cursor-pointer' %>
        </div>
      <% end %>

      <div class="pt-4 text-center">
        <%= link_to 'Back', root_path, class: 'font-medium text-indigo-600 hover:text-indigo-500' %>
      </div>

      <div class="text-center mt-6">
        <%= button_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?", turbo_confirm: "Are you sure?" }, method: :delete, class: 'inline-block bg-red-600 text-white py-2 px-4 rounded-lg hover:bg-red-700' %>
      </div>
    </div>
  </div>
</body>
</html>
