<!DOCTYPE html>
<html lang="en">
<head>
  <title>HappyLand Corp</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%# <%= stylesheet_link_tag 'application', media: 'all', 'data-turbo-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbo-track': 'reload', defer: true %>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-gray-100 text-gray-900">
  <header class="bg-white shadow">
    <div class="container mx-auto px-4 py-2 flex justify-between items-center">
      <div class="text-xl font-bold">
        <%= link_to 'HappyLand Corp', root_path %>
      </div>
      <nav class="hidden md:flex space-x-4">
        <%= link_to 'Home', root_path, class: 'text-gray-600 hover:text-gray-900' %>
        <%= link_to 'About', about_path, class: 'text-gray-600 hover:text-gray-900' %>
        <%= link_to 'Contact', contact_path, class: 'text-gray-600 hover:text-gray-900' %>
        <% Category.all.each do |category| %>
          <%= link_to category.name, category_products_path(category), class: 'text-gray-600 hover:text-gray-900' %>
        <% end %>
        <% if user_signed_in? %>
          <%= link_to 'My Orders', user_orders_path(current_user), class: 'text-gray-600 hover:text-gray-900' %>
          <%= link_to 'Sign Out', destroy_user_session_path, method: :delete, class: 'text-gray-600 hover:text-gray-900', data: {confirm: "Are you sure?"} %>
          <%# <%= link_to 'Sign Out', destroy_user_session_path, data: {turbo_method: :delete, turbo_confirm: 'Are you sure?'} %>
        <% else %>
          <%= link_to 'Sign In', new_user_session_path, class: 'text-gray-600 hover:text-gray-900' %>
          <%= link_to 'Sign Up', new_user_registration_path, class: 'text-gray-600 hover:text-gray-900' %>
        <% end %>
      </nav>
      <div class="md:hidden">
        <button id="menu-toggle" class="text-gray-600 hover:text-gray-900 focus:outline-none">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7"></path>
          </svg>
        </button>
      </div>
    </div>
  </header>

  <div id="mobile-menu" class="hidden md:hidden bg-white">
    <%= link_to 'Home', root_path, class: 'block px-4 py-2 text-gray-600 hover:bg-gray-100' %>
    <%= link_to 'About', about_path, class: 'block px-4 py-2 text-gray-600 hover:bg-gray-100' %>
    <%= link_to 'Contact', contact_path, class: 'block px-4 py-2 text-gray-600 hover:bg-gray-100' %>
    <% Category.all.each do |category| %>
      <%= link_to category.name, category_products_path(category), class: 'block px-4 py-2 text-gray-600 hover:bg-gray-100' %>
    <% end %>
    <% if user_signed_in? %>
      <%= link_to 'My Orders', user_orders_path(current_user), class: 'block px-4 py-2 text-gray-600 hover:bg-gray-100' %>
      <%= link_to 'Sign Out', destroy_user_session_path, method: :delete, class: 'block px-4 py-2 text-gray-600 hover:bg-gray-100' %>
    <% else %>
      <%= link_to 'Sign In', new_user_session_path, class: 'block px-4 py-2 text-gray-600 hover:bg-gray-100' %>
      <%= link_to 'Sign Up', new_user_registration_path, class: 'block px-4 py-2 text-gray-600 hover:bg-gray-100' %>
    <% end %>
  </div>

  <%= yield %>

  <footer class="bg-gray-800 text-white mt-8 py-4">
    <div class="container mx-auto px-4">
      <p class="text-center">&copy; <%= Time.now.year %> YourAppName. All rights reserved.</p>
    </div>
  </footer>

  <script>
    document.getElementById('menu-toggle').addEventListener('click', function() {
      var menu = document.getElementById('mobile-menu');
      if (menu.classList.contains('hidden')) {
        menu.classList.remove('hidden');
      } else {
        menu.classList.add('hidden');
      }
    });
  </script>
</body>
</html>
